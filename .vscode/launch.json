{
  "version": "0.2.0",
  "configurations": [

    {
      "name": "Debug CUDA (Nsight)",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      //"externalConsole": false
    }
  ]
}
